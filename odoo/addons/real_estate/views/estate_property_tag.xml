<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Vista list para las etiquetas de propiedades -->
    <record id="view_estate_property_tag_tree" model="ir.ui.view">
        <field name="name">estate.property.tag.tree</field>
        <field name="model">estate.property.tag</field>
        <field name="arch" type="xml">
            <tree string="Property Tag">
                <field name="name"/>
            </tree>
        </field>
    </record>
     <!-- Vista form para las etiquetas de propiedades  -->
    <record id="view_estate_property_tag_form" model="ir.ui.view">
        <field name="name">estate.property.tag.form</field>
        <field name="model">estate.property.tag</field>
        <field name="arch" type="xml">
            <form string="Property Tag">
                <sheet>
                    <group>
                        <field name="name"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
    <!-- Acción de ventana para las etiquetas de propiedades -->
    <record id="action_estate_property_tag" model="ir.actions.act_window">
        <field name="name">Property Tags</field>
        <field name="res_model">estate.property.tag</field>
        <field name="view_mode">tree,form</field>
    </record>
    <!-- Menú para las etiquetas de propiedades -->
    <menuitem id="menu_estate_property_tag" name="Property Tags" parent="menu_estate_property" action="action_estate_property_tag" sequence="20"/>
</odoo>